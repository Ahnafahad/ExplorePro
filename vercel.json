{
  "version": 2,
  "buildCommand": "cd backend && npm install --include=dev && npm run vercel-build && cd ../frontend && npm install --include=dev && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
