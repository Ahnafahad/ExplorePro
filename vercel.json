{
  "version": 2,
  "buildCommand": "cd backend && npm install && npm run vercel-build && cd ../frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
